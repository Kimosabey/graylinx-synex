"""The curated library, transcribed. Human-written content that names where it came from.

**The failure this package prevents.** A field instruction that cannot name its source is
indistinguishable from model output, and inherited constraint 1 is that the library is
curated content — never model output — because a plausible-but-wrong item directs physical
work on pressurised refrigerant equipment. Constraint 26 says the language model *selects and
contextualises* library content; it never authors a field instruction. So every object in
this package carries the file and the heading it was copied from, character for character,
and nothing in it was reworded, corrected, completed or inferred.

**Nothing here has been reviewed by a refrigeration engineer.** `sme_reviewed` is `False`
throughout, which is what keeps the content off every screen until the SME hour. It is
deliberately *not* `is_sample`: sample content is invented to demonstrate a mechanism, and
this is the real library waiting for a reader. Those are different facts and the flags stay
different.
"""
