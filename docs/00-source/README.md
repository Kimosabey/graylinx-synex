# Source documents — read only

Drop the current authoritative documents here. **Never edit a file in this
folder.** When one is superseded, move it to `docs/90-archive/` with a date
suffix and put the replacement here.

## Expected files

| File | What it is |
|---|---|
| `Graylinx_Enterprise_AI_Platform_v4.docx` | The 78-page product and architecture reference. 44 chapters in two parts plus an appendix. Still uses legacy names — the Synex rename is task T1. |
| `Graylinx_Feature_Review_Pack.pdf` / `.docx` | The 9-page landscape review pack for the SME session. |
| `WC_CHiller_FDD.docx` | The water-cooled chiller FDD specification. Source of the six models, gates, isolation path and fault fingerprints. |

## Anything else you add

Add a row to the table above saying what it is and where it came from. A file
in this folder with no explanation will be treated as untrusted and ignored.

## Precedence

If a source document disagrees with `CONTEXT.md`, `CONTEXT.md` wins and the
disagreement is raised as an open question. Do not silently follow the older
document.
